# directory_file_checker
Script that checks if a directory exists, if it does exist, see if a specific file exists within that directory. If it does not, create the file. Print whether or not the directoies or file exists, if it doesn't exist, print that it is now created.
